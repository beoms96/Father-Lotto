import 'package:flutter/material.dart';
import 'dart:math';

class TodayLotto extends StatelessWidget {
  const TodayLotto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
